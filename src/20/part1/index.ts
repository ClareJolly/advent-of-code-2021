import { batchByBlankLines } from 'src/helpers'

const part1 = (inputData: string[]) => {
  const data = batchByBlankLines(inputData)
  console.log('🚀 ~ file: index.ts ~ line 6 ~ part1 ~ data', data)
}

export default part1
